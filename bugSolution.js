```javascript
/* Ensure that Tailwind's configuration is correct in your `tailwind.config.js` file.  This includes correctly specifying the theme colors and the content paths for purging unused styles. */

/* Check for conflicting CSS rules that might be overriding the gradient styles. Use your browser's developer tools to inspect the element and see if any other styles are applied that might interfere with the gradient. */

<div class="bg-gradient-to-r from-blue-500 to-purple-500 p-4 rounded-lg shadow-md">
  <!-- Content -->
</div>
```